# Tangent testing pass 02 — 2026-09-17

- Tested revision: `f409ee74d176c9003fb26b84ea17a48035d18fbf`.
- Starting working tree: clean.
- Scope: BUG-001 clearance retest plus safe local syntax and isolated pure-helper
  checks using synthetic data.
- Tracker result: [BUG-001](../reports/BUG-001.md) remains Medium, Ready for
  retest. It is not yet Cleared.
- Additional confirmed issues: none.

## Commands and results

Commands ran from the repository root.

| Check | Exact command | Result |
| --- | --- | --- |
| Revision and tree | `git rev-parse HEAD && git status --porcelain=v1` | Revision recorded above; clean working tree |
| Python dependency availability | `python3 -c 'import importlib.util,sys; print(sys.version.split()[0]); print({name: bool(importlib.util.find_spec(name)) for name in ("pytest","polars","psycopg","yaml","fastapi","pydantic")})'` | Python 3.9.6; all six packages unavailable |
| Frontend tool availability | `node --version; npm --version; test -x web/node_modules/.bin/tsc && echo 'tsc=present' || echo 'tsc=absent'; test -x web/node_modules/.bin/vite && echo 'vite=present' || echo 'vite=absent'` | Node 20.20.2 and npm 10.8.2 present; local TypeScript and Vite executables absent |
| BUG-001 standalone retest | `python3 bugs/runs/2026-09-17-reader-check.py` | Exit 0: all 4 methods pass, including the original quoted-header reproduction |
| Broader pure-helper probe | `python3 bugs/runs/2026-09-17-tangent-check.py` | Exit 0: all 10 test methods pass |
| Python source syntax | `python3 -c 'import ast,pathlib; paths=sorted([*pathlib.Path("api").rglob("*.py"),*pathlib.Path("dashboards").rglob("*.py")]); [ast.parse(p.read_text(),filename=str(p)) for p in paths]; print(f"{len(paths)} Python files parsed successfully")'` | Exit 0: 35 files parsed successfully without importing application modules |
| Reader-suite syntax and inventory | `python3 -c 'import ast,pathlib; p=pathlib.Path("api/tests/test_reader.py"); t=ast.parse(p.read_text()); tests=[n.name for n in t.body if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef)) and n.name.startswith("test_")]; print(f"{len(tests)} reader test functions parse successfully"); print("\\n".join(tests))'` | Exit 0: all 11 test functions parse, including the three separator regressions; tests were not executed |
| Testing-artifact bytecode | `PYTHONPYCACHEPREFIX=/tmp/b2b-tangent-pass02-pycache python3 -m py_compile bugs/runs/2026-09-17-reader-check.py bugs/runs/2026-09-17-tangent-check.py` | Exit 0 |
| Pre-edit tree check | `git diff --check && git status --porcelain=v1` | Exit 0; clean |

## BUG-001 clearance decision

The original synthetic input now returns the expected semicolon, and every
method in the standalone reproduction passes. This verifies the fixed delimiter
helper directly from the current application source.

The clearance criteria also require the real Polars-backed `read_table` test to
produce two correctly named columns and one row, plus the complete existing
reader suite. Neither Polars nor pytest is installed. The suite could therefore
be parsed but not executed. BUG-001 remains **Ready for retest**, not Cleared.

## Coverage limits

The existing Python suite, complete ingestion path, frontend typecheck/build,
and browser flows remain untested because their dependencies are absent.
Dependencies were not installed.

No database interaction, account/auth operation, external network request,
paid operation, service startup, Docker operation, deployment, application
code edit, or configuration change occurred. These are coverage limitations,
not product bugs.
