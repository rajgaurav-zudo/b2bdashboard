# Tangent testing pass 01 — 2026-09-21

- Tested revision: `7855f7c4bc48801412b2c80499082a614a0d2dcb`.
- Starting working tree: clean; no pre-existing modifications.
- Scope: safe local Python syntax, isolated pure helpers, and synthetic CSV edge cases.
- Result: new [BUG-002](../reports/BUG-002.md), Medium, Open. [BUG-001](../reports/BUG-001.md) remains Ready for retest.

## Commands and results

Commands ran from the repository root. Makefile, package scripts, previous reports, helper scripts, current helper source, and reader tests were inspected before execution.

| Exact command | Outcome |
| --- | --- |
| `git status --short && git rev-parse HEAD` | Clean; revision above |
| `python3 -c 'import importlib.util,sys,pathlib; print("Python",sys.version.split()[0]); print({n:bool(importlib.util.find_spec(n)) for n in ("pytest","polars","psycopg","yaml","fastapi","pydantic")}); print({n:(pathlib.Path("web/node_modules/.bin")/n).is_file() for n in ("tsc","vite")})'` | Python 3.9.6; all six Python packages and both frontend executables absent |
| `python3 bugs/runs/2026-09-17-reader-check.py` | Exit 0; 4 methods pass, including BUG-001 original reproduction |
| `python3 bugs/runs/2026-09-17-tangent-check.py` | Exit 0; 10 methods pass |
| `python3 -c 'import ast,pathlib; paths=sorted([*pathlib.Path("api").rglob("*.py"),*pathlib.Path("dashboards").rglob("*.py")]); [ast.parse(p.read_text(),filename=str(p)) for p in paths]; print(f"{len(paths)} Python files parsed successfully"); t=ast.parse(pathlib.Path("api/tests/test_reader.py").read_text()); print(sum(isinstance(n,ast.FunctionDef) and n.name.startswith("test_") for n in t.body),"reader test functions parsed")'` | Exit 0; 35 files and 11 reader test functions parse; these are syntax checks, not execution of that suite |
| `python3 bugs/runs/2026-09-21-reader-matrix.py` | Final run exits 1: 288 cases, 27 separator failures (BUG-002), 261 separator checks pass; all 288 record counts pass |
| `git diff --check && git rev-parse HEAD && git status --short` | Exit 0; revision unchanged; at pre-report check only the new matrix script was untracked |

Existing helpers cover date boundaries, numeric validation, filters, percentages, intake-year choice, identifier quoting, change detection and gzip/plain payloads. The new matrix covers four delimiters, three line endings, two quoting modes, four header shapes, and three row counts, executing actual AST-extracted helper bodies.

The first matrix attempt revealed a fixture error: with CR line endings and minimal quoting, `csv.writer` did not quote a header containing only LF, creating a malformed CSV. The header was corrected to contain CRLF so it is quoted with every tested ending. This was not a product bug. The next attempt stopped at the first genuine separator failure; the final script collects every separator mismatch and exits nonzero after reporting totals.

## Clearance and coverage limits

BUG-001's original standalone scenario passes, but its Polars-backed `read_table` and full-suite clearance requirements cannot be verified with missing dependencies. It remains Ready for retest. BUG-002 is confirmed only at the delimiter-helper level.

No dependencies were installed. Complete ingestion, frontend typecheck/build/browser behavior, database-backed tests, and account/auth workflows remain untested. No database interaction, account/auth operation, external network request, paid operation, service startup, Docker operation, deployment, application code edit, or configuration change occurred. This limited pass does not establish complete project coverage.
