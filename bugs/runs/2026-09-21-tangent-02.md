# Tangent testing pass 02 — 2026-09-21

- Tested revision: `7855f7c4bc48801412b2c80499082a614a0d2dcb` plus pre-existing uncommitted changes listed below.
- Scope: isolated helper retests, synthetic CSV matrix and Python syntax checks.
- Result: all attempted functional checks pass; no new confirmed bugs. BUG-001, BUG-002 and BUG-003 remain Cleared based on their prior full-suite verification and no observed recurrence.
- This pass changed only this run log, the tracker and the three existing reports.

## Starting working tree

These changes all existed before the pass and were preserved:

```text
 M api/app/ingest/reader.py
 M api/tests/test_reader.py
 M bugs/BUGS.md
 M bugs/reports/BUG-001.md
 M dashboards/introducer_performance/context.md
 M dashboards/introducer_performance/ingest.py
 M dashboards/introducer_performance/metrics.py
 M dashboards/introducer_performance/tests/conftest.py
 M dashboards/introducer_performance/tests/test_metrics.py
 M web/src/api/client.ts
 M web/src/api/types.ts
 M web/src/dashboards/introducer360/FilterBar.tsx
 M web/src/dashboards/introducer360/Lower.tsx
 M web/src/dashboards/introducer360/index.tsx
 M web/src/dashboards/introducerPerformance/CourseSplit.tsx
 M web/src/dashboards/introducerPerformance/SidePane.tsx
 M web/src/dashboards/introducerPerformance/Tiles.tsx
 M web/src/dashboards/introducerPerformance/index.tsx
 M web/src/index.css
R  web/src/dashboards/introducer360/dates.ts -> web/src/ui/dates.ts
?? bugs/reports/BUG-002.md
?? bugs/reports/BUG-003.md
?? bugs/runs/2026-09-21-fix-verification.md
?? bugs/runs/2026-09-21-read-table-matrix.py
?? bugs/runs/2026-09-21-reader-matrix.py
?? bugs/runs/2026-09-21-tangent-01.md
?? dashboards/introducer_performance/migrations/005_intake_month_num.sql
?? web/src/dashboards/introducerPerformance/FilterBar.tsx
?? web/src/ui/DateRange.tsx
?? web/src/ui/FilterControl.tsx
?? web/src/ui/MultiSelect.tsx
```

## Commands and outcomes

Commands ran from the repository root. Testing rules, tracker, reports, previous
fix verification, current reader implementation/tests, matrix script and changed
performance metrics were reviewed. Existing dependency-free probes execute
selected application function bodies via AST extraction, without importing app
modules or starting services.

| Exact command | Result |
| --- | --- |
| `git status --short && git rev-parse HEAD` | Dirty tree recorded above |
| `python3 -c 'import importlib.util,pathlib; print({n:bool(importlib.util.find_spec(n)) for n in ("pytest","polars","psycopg","yaml","fastapi","pydantic")}); print({n:(pathlib.Path("web/node_modules/.bin")/n).is_file() for n in ("tsc","vite")})'` | All six Python packages and both frontend executables absent |
| `python3 bugs/runs/2026-09-17-reader-check.py` | Exit 0; all 4 methods pass, including BUG-001 reproduction |
| `python3 bugs/runs/2026-09-17-tangent-check.py` | Exit 0; all 10 methods pass |
| `python3 bugs/runs/2026-09-21-reader-matrix.py` | Exit 0; 288 cases, zero separator failures, all record counts pass (BUG-002 retest) |
| `python3 -c 'import ast,pathlib; paths=sorted([*pathlib.Path("api").rglob("*.py"),*pathlib.Path("dashboards").rglob("*.py")]); [ast.parse(p.read_text(),filename=str(p)) for p in paths]; print(f"{len(paths)} Python files parsed successfully")'` | Exit 0; 35 files parse |

The matrix tests four separators, three line endings, minimal/all quoting,
four header shapes and three row counts. Other probes cover date boundaries,
invalid numeric parameters, filter parsing, zero denominators, intake-year
choice, identifier quoting, changed fields and compressed/plain payloads.

Artifact verification: `git diff --check -- bugs` passed. The following
standard-library check passed: 26 local Markdown links resolve and all three
report statuses are Cleared, consistent with the tracker counts (3 confirmed,
3 Cleared, 0 Open/Ready for retest/Reopened).

```sh
python3 - <<'PY'
from pathlib import Path
import re
paths = [Path('bugs/BUGS.md'), *Path('bugs/reports').glob('*.md'), Path('bugs/runs/2026-09-21-tangent-02.md')]
links=0
for path in paths:
    for link in re.findall(r'\]\(([^)]+)\)', path.read_text()):
        if '://' not in link:
            assert (path.parent/link.split('#')[0]).exists(), (path,link)
            links += 1
for path in Path('bugs/reports').glob('*.md'):
    assert '- Status: Cleared' in path.read_text(), path
print(f'{links} local Markdown links resolve; 3 reports Cleared, matching tracker')
PY
```

## Status decisions and limits

[BUG-001](../reports/BUG-001.md) and [BUG-002](../reports/BUG-002.md) pass their
standalone helper reproductions. [BUG-003](../reports/BUG-003.md) was not
functionally retested: its fix requires Polars to verify actual frame names.
The previously recorded full-suite clearance evidence is retained; this pass
does not claim to have independently repeated it.

The prior read-table matrix script reports header-name failures but does not
exit nonzero for them. Its output must therefore be checked explicitly when
using it for BUG-003 verification. The script was not run or altered here.

No application code, configuration or dependency changes were made. No database,
account/auth, network, paid operation, Docker command, service startup or
deployment was performed. Full ingestion, frontend build/typecheck/browser,
database-backed metrics and newly changed UI behavior remain untested in this
pass. Missing host dependencies were not installed. This limited pass does not
establish complete project coverage.
