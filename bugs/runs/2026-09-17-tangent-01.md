# Tangent testing pass 01 — 2026-09-17

- Tested revision: `e409f958998c11a083ce54ac088fee1ff013a852`.
- Starting working tree: `AGENTS.md` and the complete `bugs/` directory were
  already untracked; no tracked application change was present.
- Scope: safe local static checks and isolated pure helpers with synthetic data.
  No application code, configuration, or dependencies were changed.
- Confirmed issues: [BUG-001](../reports/BUG-001.md), Medium, Open.
- Additional confirmed issues: none.

## Commands and results

Commands ran from the repository root.

| Check | Exact command | Result |
| --- | --- | --- |
| Revision and tree | `git rev-parse HEAD && git status --porcelain=v1` | Revision recorded above; `AGENTS.md` and `bugs/` were untracked before this pass |
| Python dependency availability | `python3 -c 'import importlib.util,sys; print(sys.version.split()[0]); print({name: bool(importlib.util.find_spec(name)) for name in ("pytest","polars","psycopg","yaml","fastapi","pydantic")})'` | Python 3.9.6; all six packages unavailable |
| Frontend tool availability | `node --version; npm --version; test -x web/node_modules/.bin/tsc && echo 'tsc=present' || echo 'tsc=absent'; test -x web/node_modules/.bin/vite && echo 'vite=present' || echo 'vite=absent'` | Node 20.20.2 and npm 10.8.2 present; local TypeScript and Vite executables absent |
| BUG-001 retest | `python3 bugs/runs/2026-09-17-reader-check.py` | Exit 1: 3 methods pass; quoted-header delimiter test fails because `,` is returned instead of `;` |
| Broader pure-helper probe | `python3 bugs/runs/2026-09-17-tangent-check.py` | Exit 0: all 10 test methods pass |
| Python source syntax | `python3 -c 'import ast,pathlib; paths=sorted([*pathlib.Path("api").rglob("*.py"),*pathlib.Path("dashboards").rglob("*.py")]); [ast.parse(p.read_text(),filename=str(p)) for p in paths]; print(f"{len(paths)} Python files parsed successfully")'` | Exit 0: 35 files parsed successfully without importing application modules |
| Testing-artifact bytecode | `PYTHONPYCACHEPREFIX=/tmp/b2b-tangent-pycache python3 -m py_compile bugs/runs/2026-09-17-reader-check.py bugs/runs/2026-09-17-tangent-check.py` | Exit 0 |
| Testing-artifact whitespace | `git diff --check -- bugs` | Exit 0 |

The broader probe executes the current function bodies extracted from application
source with Python's AST. It covers Saturday and Monday week boundaries, future
week selection, numeric bounds and invalid values, multi-team and introducer
filters, leap-day and month-end arithmetic, cross-year labels, intake validation,
zero-denominator shaping, current-intake-year selection, SQL identifier quoting,
change-field detection, and gzip/plain archive payload handling. It imports no
application modules and touches no services.

## BUG-001 retest

The original synthetic semicolon-delimited file with comma-heavy quoted fields
still makes `_sniff_separator` return a comma. The failure is reproducible at the
same application revision, so BUG-001 remains **Open**. No fix was claimed, and
the issue was not eligible to be marked Ready for retest or Cleared.

## Safety and coverage limits

The repository's Python dependencies are not installed, so the existing pytest
suite and complete Polars ingestion path could not run. The frontend dependencies
are also absent, so TypeScript checking, bundling, component tests, and browser
flows remain untested. Dependencies were not installed.

Database-backed tests, migrations, API startup, authentication/account paths,
external storage and network requests, deployment, and paid operations were not
run. These are coverage limitations, not product bugs. The 10 passing helper
methods establish only their listed isolated behavior and do not claim complete
project coverage.
